name: StudentReady quality

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 12 1,15 * *"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run test:quality
      - run: npm run build
      - run: npm run check:links
